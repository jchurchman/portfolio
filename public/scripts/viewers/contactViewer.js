'use strict';

var app = app || {}; // eslint-disable-line

(function (module) {
    const contactView = {};

    // this Viewer will have script that will handle the contact form sliders

    module.contactView = contactView;
}(app));