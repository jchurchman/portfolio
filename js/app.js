'use strict';

function Project(title, link, thumbnail, date, pitch, blurb){
    this.title = title;
    this.link = link;
    this.thumbnail = thumbnail;
    this.date = date;
    this.pitch = pitch;
    this.blurb = blurb;
}